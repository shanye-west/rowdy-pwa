// RedirectCountdown component removed — use explicit "Go Home" buttons in routes.
// Kept as a noop export to avoid accidental import breakages during transition.
export default function RedirectCountdown() {
  return null;
}
